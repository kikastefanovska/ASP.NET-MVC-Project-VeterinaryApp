@model VeterinaryApp.Models.Vaccine

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Vaccine</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
   @*  <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a> *@
    <form asp-action="Edit" asp-route-id="@Model?.Id" method="post" style="display: inline;">
        <button type="submit" class="btn btn-primary">Edit</button>
    </form>

    <form asp-action="Index" method="get" style="margin-left: 10px;">
        <button type="submit" class="btn btn-secondary">Back to List</button>
    </form>

</div>
